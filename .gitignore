# ==========================================================================
# Privacy: NEVER commit these - they contain visitor data (GDPR)
# ==========================================================================
logs/*
!logs/.htaccess
*.log

# Cached API responses - regenerated at runtime
cache/*
!cache/.htaccess

# Per-deployment configuration (copy the .sample files and adjust)
includes/config.php
sitemap.xml
robots.txt

# Search engine site verification (deployment-specific)
google*.html

# Binaries - the app zip comes from the SteamWorkshopManager releases
downloads/*.zip

# Old website version (kept on the server only)
legacy/

# Environment / IDE / system
.env
.vscode/
.idea/
.DS_Store
Thumbs.db
*.bak
